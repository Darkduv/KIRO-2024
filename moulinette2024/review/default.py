"""Review method placeholder."""

# import matplotlib.pyplot as plt


def review(instance, solution):
    """Do nothing except raise an error on unfound solution."""
    # =========================
    # example of last hashcode :
    # ==========================
    # scores = []
    # for shipper in solution.shippers:
    #     books = sum([instance.books[b].score for b in shipper.books])
    #     signup = instance.libraries[shipper.name].signup
    #     scores.append(books / signup)
    # plt.plot(scores)
    # plt.show()
    del instance, solution
